# Benchmark Matrix

The benchmark matrix is now part of the canonical results page:
[`RESULTS.md`](RESULTS.md) — see the *Evidence coverage* section for which controller/scenario cells are indexed only versus published with numeric values.

This file is kept as a redirect so existing links and outreach posts keep working.
